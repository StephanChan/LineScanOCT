name: python311_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=2.4
  - scipy>=1.17
  - pyqt=5
  - opencv
  - tifffile
  - imagecodecs
  - matplotlib
  - pip
  - pip:
      # Optional GPU package, adjust CUDA version if needed
      - cupy-cuda12x